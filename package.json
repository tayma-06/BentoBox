{
  "name": "bentobox",
  "version": "1.0.0",
  "description": "BentoBox - The most random thing I built in the most random time. A bento-box gallery of my favorite animated movies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "fetch:movies": "node scripts/fetch-movies.mjs",
    "resolve:spotify": "node scripts/resolve-spotify.mjs",
    "check": "node --check server.js && node --check public/app.js && node --check public/audio.js && node --check public/soundtrack.js && node --check public/spotify.js && node --check public/cursor.js && node --check scripts/fetch-movies.mjs && node --check scripts/resolve-spotify.mjs"
  },
  "keywords": [
    "bentobox",
    "animation",
    "movies",
    "webtoon",
    "express"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
